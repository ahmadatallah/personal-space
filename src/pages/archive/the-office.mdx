---
title: 'The Office'
date: 2021-05-19
---

import GalleryWrapper from '../../components/gallery-wrapper';
import { graphql } from 'gatsby';

export const query = graphql`
  query theOffice {
    images: allFile(
      filter: { relativeDirectory: { eq: "the-office" } }
      sort: { fields: name }
    ) {
      edges {
        node {
          childImageSharp {
            thumb: gatsbyImageData(layout: CONSTRAINED, width: 500)
            full: gatsbyImageData(layout: FULL_WIDTH)
          }
          name
        }
      }
    }
  }
`;

<GalleryWrapper
  images={props.data.images.edges.map(({ node }) => ({
    ...node.childImageSharp,
    caption: node.name,
    thumbAlt: node.name,
  }))}
/>

Day at the office. Kinda wild
