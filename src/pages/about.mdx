import { GatsbyImage } from 'gatsby-plugin-image';
import { graphql } from 'gatsby';
import { Container } from '../blocks';
import SEO from '../components/seo';

export default Container;

export const query = graphql`
  query {
    main: file(name: { eq: "about-img" }) {
      childImageSharp {
        gatsbyImageData(layout: CONSTRAINED, width: 500)
      }
    }
  }
`;

# About

<SEO title="About" />
<GatsbyImage image={props.data.main.childImageSharp.gatsbyImageData} />

I'm Ahmad Atallah, a programmer and an artist currently living in Germany. My hobbies include cinematic photography and generative art. I also enjoy cooking, playing tennis, watching movies, and attending cultural events.
