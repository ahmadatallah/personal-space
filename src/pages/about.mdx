import { GatsbyImage } from 'gatsby-plugin-image';
import { graphql } from 'gatsby';
import { Container } from '../blocks';
import SEO from '../components/seo';

export default Container;

export const query = graphql`
  query {
    main: file(name: { eq: "about-img" }) {
      childImageSharp {
        gatsbyImageData(layout: CONSTRAINED, width: 500)
      }
    }
  }
`;

# About

<SEO title="About" />
<GatsbyImage image={props.data.main.childImageSharp.gatsbyImageData} />

I'm Ahmad Atallah, a programmer and an artist currently living in Germany. I've worked across different industries — Millensys (healthcare), Swvl (transportation & ride-sharing), Aeditive (robotics), and Getsafe (insurance) — and I'm currently spending time on the development of my app. One of my biggest interests now is user experience, animation, and calligraphy. My hobbies include cinematic photography and generative art. I also enjoy cooking, playing tennis, watching movies, and attending cultural events.
