import displacementVideo from '../../images/motion/displacement-gold.mp4';

<div
  style={{
    position: 'relative',
    width: '100vw',
    height: 'calc(100vh - 160px)', // Adjust for header and footer
    overflow: 'hidden',
    marginLeft: 'calc(-50vw + 50%)',
    marginRight: 'calc(-50vw + 50%)',
    backgroundColor: '#000',
  }}
>
  <video
    src={displacementVideo}
    controls
    autoPlay
    loop
    muted
    playsInline
    style={{
      width: '100%',
      height: '100%',
      objectFit: 'cover',
    }}
  />
</div>
