import React from "react"
import theme from "../theme"
import Svg from '../elements/Svg'
import Path from '../elements/Path'

const Logo = ({ ...props}) => {
  return (
    <Svg viewBox="0 0 394.62 155.75" {...props}>
      <title>Logo</title>
      {/* TODO: Add Logo */}
    </Svg>

  )
}

Logo.defaultProps = {
}

export default Logo

